#include "main.h"

/**
 * mul - Prints the multiplication of two integers
 * @a: The 1st integer to be checked.
 * @b: The 2nd integer to be checked.
 *
 * Return: 1 if c is a digit, 0 otherwise
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
