#include "main.h"

/**
 * print_square - Prints square when n is initialized.
 * @size: The integer to be checked.
 *
 * Return: void
 *
 */

void print_square(int size)
{
	int i, j;

	if (size <= 0)
		_putchar('\n');

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < I; j++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}
}
